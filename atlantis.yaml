version: 3

projects:
  - name: dev
    dir: environments/dev
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true

  - name: staging
    dir: environments/staging
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true

  - name: prod
    dir: environments/prod
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
